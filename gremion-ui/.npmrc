# Enforce the packageManager field in package.json (pnpm@10.33.0).
# Prevents accidental use of pnpm 10.31.x which has broken publish, nested run,
# and JSR update regressions. pnpm 11 beta is also blocked.
engine-strict=true
